#include "main.h"

/**
 *  * rev_string - a function that reverse a string
 *   * @s: an input string
 *    * Return: Nothing
*/
void rev_string(char *S)
{
	int a = 0, b = 0;
	char rev;

	while (s[a] != '\0')
		a++;

	while (b < a--)
	{
		rev = s[b]
			s[b++] = s[a];
		s[a] = rev
	}
}
